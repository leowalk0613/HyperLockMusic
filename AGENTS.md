# HyperLockMusic — Agent 工作规范

面向在本仓库中改动代码的 AI / 协作者。与 `.cursorrules` 一并遵守；冲突时以本文件的交付门禁为准。

## 强制交付门禁（每次改动）

完成任何功能修复、行为变更、重构或配置调整后，**在回复用户“已完成”之前**必须全部做到：

1. **测试** — 为本次改动编写或更新对应测试（单元 / 相关验证脚本均可）；无现成测试目录时，先补齐可运行的最小测试再改业务逻辑。
2. **验证通过** — 运行相关测试与必要的构建/验证命令，确认全部通过；失败不得交付。
3. **Git commit** — 为本次改动创建对应 commit，便于追踪与回滚。一条逻辑改动对应一个清晰 commit；勿把无关改动混进同一提交。

禁止：只改代码不写测试、测试未跑或未通过就声称完成、改完不提交就结束会话任务。

### 建议工作流

```text
改代码 → 写/更新测试 → 跑测试与验证 → 全部通过 → git commit → 再向用户汇报
```

### Commit 要求

- 信息聚焦「为什么」，简洁准确（fix / add / update 等与实际性质一致）。
- 不提交密钥、`.env`、本地凭证等敏感文件。
- 用户明确要求 push / 开 PR 时再推远程；默认只做本地 commit。

## Android 构建（CLI）

- 一律使用项目 Wrapper：`./gradlew`（Windows：`.\gradlew.bat`）。
- 不要依赖 Android Studio 做编译、同步、安装或排错。
- 常用：`assembleDebug`、`test`、`connectedAndroidTest`（若已配置）、`:app:installDebug`。
- SDK 路径从 `.env` 或环境变量 `ANDROID_HOME` / `ANDROID_SDK_ROOT` 读取。
- 本机绝对路径（SDK / ADB / 反编译 / 效果图等）仅放在被 gitignore 的 `.cursor/rules/`，不要写进本文件或推远程。

## 本项目注意点

- 目标环境：HyperOS 4 + LSPosed；钩子多落在 SystemUI / AOD / 媒体会话路径。
- **切歌 / 媒体切换** 属于高回归风险区：改动媒体控件、通知、封面、歌词同步时，必须有可重复的测试或明确的验证步骤，并单独 commit，避免与无关 UI 改动绑在一起。

## 代码改动原则

- 只改任务所需文件；避免顺手大重构与无关格式化。
- 与现有命名、风格、错误处理保持一致。
- 不擅自新增用户未要求的 markdown/文档（本 `AGENTS.md` 除外）。
